export interface IContactRequest {
  full_name: string;
  phone_number: string;
  email: string;
  user_id: string;
}

export interface IContact {
  id: string;
  full_name: string;
  phone_number: string;
  email: string;
}
